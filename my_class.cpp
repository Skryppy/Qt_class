#include "my_class.h"

#include "mainwindow.h"
#include "ui_mainwindow.h"

My_class::My_class()
{
//    connect(&a, SIGNAL(setList(const QStringList &)), SLOT(setList(const QStringList &)));
//    connect(this, SIGNAL(sendData(QString)), my_class, SLOT(recieveData(QString)));

//    MainWindow * form1;
//    connect( form1, SIGNAL(sendData(QString &)), SLOT(recieveData(QString &)));

}

void My_class::recieveData(QString str)
{
    QString text = str;

}
